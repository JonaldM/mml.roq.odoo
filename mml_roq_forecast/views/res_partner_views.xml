<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_res_partner_roq_ext" model="ir.ui.view">
        <field name="name">res.partner.roq.ext</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="ROQ / Freight" name="roq_freight_tab"
                      attrs="{'invisible': [('supplier_rank', '=', 0)]}">
                    <group>
                        <group string="Freight">
                            <field name="purchase_incoterm_id"
                                   placeholder="FOB assumed if not set"/>
                            <field name="fob_port_id"
                                   placeholder="Select origin port"/>
                            <field name="destination_port_id"
                                   placeholder="Select NZ destination port"/>
                            <field name="free_days_at_origin"
                                   placeholder="0"/>
                            <field name="avg_lead_time_actual" readonly="1"/>
                            <field name="lead_time_std_dev" readonly="1"/>
                            <field name="lead_time_on_time_pct" readonly="1"/>
                        </group>
                        <group string="ROQ Parameter Overrides">
                            <field name="supplier_lead_time_days"
                                   placeholder="Leave blank = system default"/>
                            <field name="supplier_review_interval_days"
                                   placeholder="Leave blank = system default"/>
                            <field name="supplier_service_level"
                                   placeholder="Leave blank = ABC tier rate"/>
                            <field name="override_expiry_date"/>
                            <field name="supplier_holiday_periods"
                                   placeholder='[{"start": "2026-01-28", "end": "2026-02-10", "reason": "CNY"}]'/>
                        </group>
                    </group>
                </page>
            </notebook>
        </field>
    </record>

    <!-- Action and menu for managing freight ports -->
    <record id="action_roq_port" model="ir.actions.act_window">
        <field name="name">Freight Ports</field>
        <field name="res_model">roq.port</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="view_roq_port_list" model="ir.ui.view">
        <field name="name">roq.port.list</field>
        <field name="model">roq.port</field>
        <field name="arch" type="xml">
            <list string="Freight Ports">
                <field name="code" string="UN/LOCODE"/>
                <field name="name" string="Port Name"/>
                <field name="country_id" string="Country"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_roq_port_form" model="ir.ui.view">
        <field name="name">roq.port.form</field>
        <field name="model">roq.port</field>
        <field name="arch" type="xml">
            <form string="Freight Port">
                <sheet>
                    <group>
                        <field name="code" string="UN/LOCODE"
                               placeholder="5-char code, e.g. CNSHA"/>
                        <field name="name" string="Port Name"
                               placeholder="e.g. Shanghai"/>
                        <field name="country_id"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
